# 🚀 FastAPI App Settings
APP_NAME=TravelCRM
APP_ENV=production # Changed for server deployment context
APP_PORT=8000

# 🛢️ MySQL Database: Removed obsolete local DB variables.
# The connection details are now managed by database.py (for flexibility) 
# and docker-compose.yml (for credentials and service name 'db').

# 🔐 JWT Auth 
JWT_SECRET_KEY=your_super_secret_key # **Replace this with a long, random key!**
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# 📬 Email Settings 
EMAIL_HOST=smtp.yourdomain.com
EMAIL_PORT=587
EMAIL_USER=your_email@yourdomain.com
EMAIL_PASSWORD=your_email_password
EMAIL_FROM=Travel CRM <your_email@yourdomain.com>

# 🧭 Webhook Settings (optional)
WEBHOOK_URL=https://your-webhook-endpoint.com

# 🧠 Sentry (optional error tracking)
SENTRY_DSN=https://your-sentry-dsn

# 🌍 CORS
ALLOWED_ORIGINS=http://localhost,http://127.0.0.1,https://api.yaadigo.com