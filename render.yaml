services:
  - name: fastapi-app
    env: python
    pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn Travel_CRM_backend.main:app --host 0.0.0.0 --port 8000

